1.Create Role without users (only so);
2.!Create new Role with new User (user.id==null);

3.Create User without roles;
4.Create User with existing roles (role.id!=null);
5.Modified User: add/remove existing roles (role.id=null) - special methods;
6.!Create new User with new Role (role.id==null);

7.Block user;
8.Find User by email/Role by name;

9.Verifying the User email to confirm their account.

TODO ADMIN IS BLOCKED DUE TO MULTIPLE WRONG ATTEMPTS...